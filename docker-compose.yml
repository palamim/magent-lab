services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: magent
      POSTGRES_PASSWORD: magent
      POSTGRES_DB: magent_lab
    ports:
      - '5432:5432'
    volumes:
      - magent_lab_data:/var/lib/postgresql/data

volumes:
  magent_lab_data:
